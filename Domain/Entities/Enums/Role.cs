namespace Domain.Entities.Enums
{
    // Перерахування, що визначає ролі користувачів у системі.
    public enum Role
    {
        Regular, // звичайний користувач
        Manager  // має доступ до створення питань
    }
}